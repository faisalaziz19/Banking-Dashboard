import React from "react";
import { Box, Typography } from "@mui/material";

function Content() {
  return (
    <Box sx={{ mt: 2 }}>
      {/* Add charts, tables, or other content */}
      <Typography variant="h4">Dashboard Content</Typography>
    </Box>
  );
}

export default Content;
